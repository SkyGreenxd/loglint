# loglint

<div align="center">
  <a href="https://go.dev/"><img src="https://img.shields.io/badge/Go-00ADD8?style=for-the-badge&logo=go&logoColor=white" alt="Go"></a>
  
  **Cпециализированный линтер для Go, который помогает поддерживать стандарты оформления сообщений в логах**
  
  **Совместим с `golangci-lint`**
</div>

---

`loglint` автоматически проверяет текст сообщений на соответствие правилам (регистр, пунктуация, отсутствие чувствительных данных) для популярных библиотек логирования.

## Особенности

- **Производительность**: Использует стандартный фреймворк `go/analysis`.
- **Гибкость**: Легко настраиваемые правила и уровни логирования (INFO, WARN, ERROR).
- **Масштабируемость**: Архитектура проекта спроектирована для легкого добавления новых правил и поддержки сторонних логгеров.
- **Интеграция**: Работает как самостоятельная утилита или как плагин для `golangci-lint`.
- **Поддержка логгеров**: Из коробки поддерживает `zap` и `slog`.

## Поддерживаемые логгеры

- **Uber Zap**: `zap.Logger`, `zap.SugaredLogger`.
- **Slog**: Стандартный пакет `log/slog`.

## Правила проверки (Rules)

| Название    | Описание                                                                                                                            |
| ----------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| `lowercase` | Проверяет, что сообщение лога начинается со строчной буквы.                                                                         |
| `english`   | Гарантирует, что сообщение написано только на английском языке.                                                                     |
| `symbols`   | Запрещает использование спецсимволов, пунктуации и эмодзи в сообщении.                                                              |
| `sensitive` | Поиск потенциально чувствительных данных (пароли, токены) в сообщениях по ключевым словам. **Настраивается через `.golangci.yml`**. |

## ⚙️ Конфигурация

Вы можете настроить правила и логгеры через файл `.golangci.yml`. Особое внимание стоит уделить правилу `sensitive`, для которого можно задать собственные регулярные выражения (иначе используется дефолтный набор паттернов):

```yaml
linters-settings:
  custom:
    loglint:
      settings:
        rules:
          sensitive:
            enabled: true
            severity: ERROR
            options:
              patterns:
                - "(?i)secret"
                - "(?i)password"
```

Дефолтный набор паттернов для `sensitive`

```go
`(?i)\b(password|passwd|pwd)\b`
`(?i)\b(api[_-]?key|apikey)\b`
`(?i)\b(token|auth[_-]?token)\b`
`(?i)\b(secret|secret[_-]?key)\b`
`(?i)\b(access[_-]?key|accesskey)\b`
`(?i)\b(credentials?|creds?)\b`
`Bearer\s+[A-Za-z0-9\-._~+/]+=*`
`[0-9a-f]{32,}`
```

## 🚀 Быстрый старт

### Сборка и запуск линтера

Эта команда автоматически установит `golangci-lint` (если он не установлен), соберет кастомный бинарник с плагином и запустит проверку:

```bash
make
```

### Запуск тестов

```bash
make test
```

### Очистка артефактов

```bash
make clean
```

## Использование

### Самостоятельная утилита (CLI)

Если вы хотите использовать `loglint` без `golangci-lint`:

```bash
go install github.com/SkyGreenxd/loglint/cmd/loglint@latest
loglint ./...
```

### Интеграция в golangci-lint (как плагин)

Добавьте следующую конфигурацию в ваш `.golangci.yml`:

```yaml
linters-settings:
  custom:
    loglint:
      path: .bin/loglint.so
      description: Linter for log messages
      original-url: github.com/SkyGreenxd/loglint

linters:
  enable:
    - loglint
```

> Примечание: Вам нужно собрать плагин с помощью `go build -buildmode=plugin` перед использованием.
